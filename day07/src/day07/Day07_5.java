package day07;

public class Day07_5 {
public static void main(String[] args) {
	/*
	 클래스는 public, default 접근 지정자만 사용할 수 있다
	 클래스를 default로 선언하면 다른 패키지 에서는 import가 불가능해진다
	 public 으로 선언된 클래스만 다른 패키지에서 import가 가능해진다
	 */
}
}
